/**
 * Pi extension bundle: agent-team
 *
 * Keeps the `agent-team` folder discoverable under `~/.pi/agent/extensions` (symlinked to this repo).
 * Team membership is defined in `.pi/agents/teams.yaml` — not in this file.
 *
 * Pi extension docs: https://github.com/earendil-works/pi-mono/blob/main/packages/coding-agent/docs/extensions.md
 */
export default function () {}
