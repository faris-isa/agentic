* Break the **approved plan or PRD** into **small, independently actionable work items** (vertical slices).
* Each item should have a clear outcome, rough scope, and acceptance notes.
* Order items to respect dependencies; call out what can run in parallel.
* If using GitHub, prefer titles and bodies that paste cleanly into issues (`gh issue create`).
